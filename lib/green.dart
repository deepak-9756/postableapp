import 'package:flutter/material.dart';

class GreenClass extends StatelessWidget {
  const GreenClass({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
